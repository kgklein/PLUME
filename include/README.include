The <include> directory is where the Makefile will store the FORTRAN modules after compilation.

If files of the form *.o and *.mod are in the parent directory after compilation, navigate to the pareent directory and call:
make tidyup
